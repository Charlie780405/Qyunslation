# PLAN-008：文档类型排版模板与段落级 OCR 保真

## 目标

修复扫描件译文句子被行级切分腰斩；按文档类型（书信 / 文献 / IND 递交）切换字体族与排版参数。GUI 下拉手动选 + 自动识别给推荐值。

## 子计划

| 编号 | 名称 | 依赖 |
| --- | --- | --- |
| 008a | 完整性度量夹具 | — |
| 008b | HPD 行→段落聚合 | 008a |
| 008c | 样例模板 toml + detect/apply | 008b |
| 008d | GUI 下拉与运行时行距 | 008c |
| 008e | 页 1 验收与样例 PNG | 008b+008c+008d |

## 质量不变量

- **不改泰州 HPD 服务**（`100.67.66.123:8120`）。只改本仓库 `scripts/hpd_ocr.py` 本地后处理。
- 字体只用 BabelDOC 内置思源黑体/思源宋体（`primary_font_family`）。
- `ocr_workaround` 仍仅 HPD 分支启用，不写进 `config.toml` 全局。
- 本 PLAN 只验第 1 页；全量 20 页与真实文献/IND 样本另开 PLAN-009。

## Out of Scope

- 自定义 ttf/otf、两端对齐、段前段后距
- 修改泰州 HPD-MCP / 其它消费方
- 全量 PIND 20 页重跑
